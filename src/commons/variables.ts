// Error type prefixes
const invalidInputPrefix: string = "|:Invalid_Input:|";

// http response message texts
const requiredParamsNotFound: string = "Required_Params_Not_Found";

// defaults
const defaultPort: number = 5000;

export {
    invalidInputPrefix,
    requiredParamsNotFound,
    defaultPort
};